<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:scrollbarThumbVertical">@color/mi_color_de_barra</item>


    </style>


    <style name="AppTheme.AppBarOverlay" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppThemeTwo" parent="Theme.AppCompat.Light">
        <item name="android:colorControlActivated">#ffffff</item>
        <item name="android:colorControlNormal">#4CAF50</item>
    </style>

    <style name="toolbarbig" parent="@style/Base.TextAppearance.AppCompat.Title">
        <item name="android:textSize">36dp</item>
    </style>
    <!-- Estilos de Dialog Picker -->
        <!-- Estilo para texto pequeño (menor a 480dp) -->
        <style name="DialogTextStyleSmall">
            <item name="android:textSize">18sp</item>
        </style>
        <!-- Estilo para texto mediano (480dp a 720dp) -->
        <style name="DialogTextStyleMedium">
            <item name="android:textSize">24sp</item>
        </style>
        <!-- Estilo para texto grande (mayor a 720dp) -->
        <style name="DialogTextStyleLarge">
            <item name="android:textSize">40sp</item>
        </style>







</resources>
